{
  "name": "demo-app",
  "root": "apps/demo-app",
  "targets": {
    "watch": {
      "options": {
        "commands": ["vite"]
      }
    },
    "build": {
      "options": {
        "commands": ["tsc", "vite build"]
      }
    },
    "lint": {},
    "format": {}
  }
}
